---
// Section 15: Peaceful Separation — Hayek's Vision
// Arc III: The Way Out

const title = "Peaceful Separation — Hayek's Vision"
const content = "Economist Friedrich Hayek argued the best way to improve money is competition. We don't fight the old system — we simply choose a better one and let it win."
const resourceLink = ""
const resourceText = "Read Hayek's Denationalisation of Money"
---

<!-- Section Content - Slot content for SectionWrapper -->
<div class="flex flex-col justify-center items-center h-full py-8 px-4 sm:px-6 lg:px-8">
  <div class="max-w-4xl mx-auto text-center">
    <h2 class="bs-h2 mb-6">{ title }</h2>
    <p class="bs-body-text text-xl text-bs-foreground-dark mb-6">{ content }</p>
    {resourceLink && (
      <a href={resourceLink} target="_blank" rel="noopener" class="bs-btn">
        { resourceText }
      </a>
    )}
  </div>
</div>

<style>
  /* Section-specific styles */
  :global(.section-wrapper:has(#section-15)) {
    background: linear-gradient(135deg, var(--color-cypherpunk-gray) 0%, var(--color-bitcoin-dark) 50%, var(--color-cypherpunk-gray) 100%);
  }
</style>

