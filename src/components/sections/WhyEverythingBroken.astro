---
// Section 06: Why Does Everything Feel So Broken?
// Arc I: The Struggle

const title = "Why Does Everything Feel So Broken?"
const content = "This short video captures exactly how millions feel right now — and sets up the search for the real reason."
const videoUrl = ""
const videoTitle = ""
---

<!-- Section Content - Slot content for SectionWrapper -->
<div class="flex flex-col justify-center items-center h-full py-8 px-4 sm:px-6 lg:px-8">
  <div class="max-w-4xl mx-auto text-center">
    <h2 class="bs-h2 mb-6">{ title }</h2>
    <p class="bs-body-text text-xl text-bs-foreground-dark mb-6">{ content }</p>
    {videoUrl && (
      <div class="aspect-video w-full max-w-3xl mx-auto">
        <iframe
          width="100%"
          height="100%"
          src={videoUrl}
          title={videoTitle}
          frameborder="0"
          allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture"
          allowfullscreen
          loading="lazy">
        </iframe>
      </div>
    )}
  </div>
</div>

<style>
  /* Section-specific styles */
  :global(.section-wrapper:has(#why-everything-broken)) {
    background: linear-gradient(135deg, var(--color-cypherpunk-darker) 0%, var(--color-cypherpunk-dark) 100%);
  }
</style>

